/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com'], // Thêm dòng này để cho phép hình ảnh từ vi placeholder
    },
};

export default nextConfig;

